{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://claude-subagents.local/schemas/skill-frontmatter.schema.json",
  "title": "Skill Frontmatter",
  "description": "Schema for YAML frontmatter at the top of SKILL.md files. Matches official Claude Code plugin format. Contains skill identity and optional runtime behavior.",
  "type": "object",
  "required": ["name", "description"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "Skill identifier. Use format 'name (@author)' (e.g., 'react (@vince)'). Simple kebab-case names like 'react' are also supported for plugins.",
      "minLength": 1,
      "examples": [
        "react (@vince)",
        "hono (@vince)",
        "zustand (@vince)",
        "react"
      ]
    },
    "description": {
      "type": "string",
      "description": "Brief description of the skill's purpose for Claude agents",
      "minLength": 1,
      "examples": [
        "React patterns and conventions for Claude agents",
        "Hono API routes with OpenAPI/Zod validation"
      ]
    },
    "disable-model-invocation": {
      "type": "boolean",
      "description": "If true, prevents the AI model from invoking this skill. Default: false",
      "default": false
    },
    "user-invocable": {
      "type": "boolean",
      "description": "If true, users can invoke this skill directly. Default: true",
      "default": true
    },
    "allowed-tools": {
      "type": "string",
      "description": "Comma-separated list of tools this skill can use (e.g., 'Read, Grep, Glob')",
      "examples": ["Read, Grep, Glob", "Read, Write, Bash"]
    },
    "model": {
      "type": "string",
      "description": "AI model to use for this skill",
      "enum": ["sonnet", "opus", "haiku", "inherit"],
      "examples": ["sonnet", "opus"]
    },
    "context": {
      "type": "string",
      "description": "Context mode for skill execution",
      "enum": ["fork"],
      "examples": ["fork"]
    },
    "agent": {
      "type": "string",
      "description": "Agent name to use when skill is invoked",
      "examples": ["Explore", "Code"]
    },
    "argument-hint": {
      "type": "string",
      "description": "Hint for arguments when skill is invoked",
      "examples": ["<file_path>", "<query>"]
    }
  },
  "examples": [
    {
      "name": "react",
      "description": "React patterns and conventions for Claude agents"
    },
    {
      "name": "api-hono",
      "description": "Hono API routes with OpenAPI/Zod validation",
      "allowed-tools": "Read, Grep, Glob",
      "model": "sonnet"
    },
    {
      "name": "security",
      "description": "Security best practices and vulnerability prevention",
      "disable-model-invocation": false,
      "user-invocable": true
    }
  ]
}
