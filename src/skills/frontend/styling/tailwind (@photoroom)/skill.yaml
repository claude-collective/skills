# yaml-language-server: $schema=../../../schemas/skill.schema.json
id: frontend/tailwind (@photoroom)
name: Tailwind CSS
description: Tailwind CSS, clsx, design tokens for Photoroom webapp
category: styling
category_exclusive: true
author: "@photoroom"
version: "1.0.0"

compatible_with:
  - frontend/react-mobx (@photoroom)

conflicts_with:
  - frontend/styling

tags:
  - tailwind
  - css
  - clsx
  - design-tokens
  - photoroom
